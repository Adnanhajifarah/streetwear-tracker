# depop-price-tracker

Reads title, price, brand, and availability out of saved Depop listing pages.

Depop sits behind bot protection that blocks automated requests, so this tool
does not fetch pages itself. You save a listing from your own browser and point
the tool at the saved HTML. Parsing is the interesting part anyway.

Work in progress.
